### coincap
------------
##### 1. clone repository
##### 2. install dependencies:
`$ npm install && bower install`
##### 3. run the app:
`$ npm start`
##### 4. magic happens on http://localhost:3000/
